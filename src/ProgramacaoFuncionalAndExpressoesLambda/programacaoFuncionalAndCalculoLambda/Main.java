package ProgramacaoFuncionalAndExpressoesLambda.programacaoFuncionalAndCalculoLambda;

/*
 * O que são "expressões lambda"?
 * Em programação funcional, expressão lambda corresponde a uma função anônima de primeira classe.
 */

public class Main {

/*    public static int comparteProducats(Product p1, Product p2) {
        return p1.getPrice().compareTo(p2.getPrice());
    }

    public static void main(String[] args) {
        (...)
        list.sort(Program::compareProducts);
        list.sort((p1, p2) -> p1.getPrice().compareTo(p2.getPrice()));
    }
*/
}
